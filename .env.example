# Database Configuration
# For local development with Docker, these values match docker-compose.yml defaults
DATABASE_URL="postgresql://dev:devpassword@localhost:5432/aragon_dev?schema=public"

# Application Configuration
PORT=3000
NODE_ENV=development

# PostgreSQL Configuration (for docker-compose)
# ⚠️ These are LOCAL DEVELOPMENT defaults - change for production!
POSTGRES_USER=dev
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=aragon_dev
POSTGRES_PORT=5432
